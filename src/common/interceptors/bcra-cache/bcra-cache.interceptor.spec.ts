import { BcraCacheInterceptor } from './bcra-cache.interceptor';

describe('BcraCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new BcraCacheInterceptor()).toBeDefined();
  });
});
